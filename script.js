function toggleMode() {
  const html = document.documentElement
  html.classList.toggle("light")
  //somente esse a cima faz a mesma coisa doque o codigo abaixo

  //pegar a tag img
  //const img = document.querySelector("#profile img")

  //substituir a foto
  //if (html.classList.contains("light")) {
  // se tiver light mode, adiciona a foto light
  //img.setAttribute("src", "./Assest/assets/avatarlight.png.png")
  //} else {
  // set tiver sem light mode, manter a foto normal
  // img.setAttribute("src", "./Assets/assets/avatar.png.png")
  // }
}
